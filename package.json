{
  "name": "hl7.fhir.us.medicaid.plannet",
  "version": "0.1.0",
  "description": "FHIR Implementation Guide for Medicaid Provider Directory functionality based on Da Vinci Plan-Net",
  "author": "HL7 Financial Management Working Group",
  "license": "CC0-1.0",
  "devDependencies": {
    "fsh-sushi": "^3.14.0"
  },
  "scripts": {
    "build": "sushi build",
    "ig:build": "sushi build && _updatePublisher.sh && _genonce.sh",
    "ig:publish": "sushi build && _updatePublisher.sh && _genonce.sh"
  },
  "dependencies": {
    "path": "^0.12.7"
  }
}
